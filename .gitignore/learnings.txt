* auto_adjust= True tells Yahoo finance to automatically adjust the stock prices for stock splits, bonus shares and dividends

* import matplotlib.pyplot as plt 

    df = all_data["RELIANCE.NS"]

    plt.figure(figsize = (12,5))
    plt.plot(df.index, df["Close"])
    plt.title("Reliance closing price")
    plt.xlabel("Date")
    plt.ylabel("Price")
    plt.show

    Here in plt.plot -> df.index means date column which acts as index for the dataframe

* .squeeze() -> converts a single column dataframe into a series

* .dropna() removes every rows containing atleast one NaN value